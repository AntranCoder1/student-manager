extends ../layouts/layout.pug

block content
    h1.user_list Create Users 

    form.users_search(action="/users/create", method="post")
        input(type="text", name="name", placeholder="name")
        button Create